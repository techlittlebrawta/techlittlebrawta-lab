---
- name: Run approved per-platform validation
  hosts: mbj_community_control
  gather_facts: false
  vars:
    runner_playbook: platform_validate
    runner_limit: pnetlab
  tasks:
    - import_tasks: tasks/community_runner_request.yml
