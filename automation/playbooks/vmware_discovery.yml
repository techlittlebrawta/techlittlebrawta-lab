---
- name: Safely discover standalone ESXi facts
  hosts: virtualization
  gather_facts: false
  connection: local
  vars:
    vmware_validate_certs: "{{ lookup('env', 'VMWARE_VALIDATE_CERTS') | default('false', true) | bool }}"

  tasks:
    - name: Require VMware credential injection
      ansible.builtin.assert:
        that:
          - lookup('env', 'VMWARE_USER') | length > 0
          - lookup('env', 'VMWARE_PASSWORD') | length > 0
        fail_msg: Attach the TLB VMware API credential to this job template.
      no_log: true

    - name: Gather standalone ESXi host facts
      community.vmware.vmware_host_facts:
        hostname: "{{ ansible_host }}"
        username: "{{ lookup('env', 'VMWARE_USER') }}"
        password: "{{ lookup('env', 'VMWARE_PASSWORD') }}"
        validate_certs: "{{ vmware_validate_certs }}"
      no_log: true

    - name: Read standalone ESXi role definitions
      community.vmware.vmware_local_role_info:
        hostname: "{{ ansible_host }}"
        username: "{{ lookup('env', 'VMWARE_USER') }}"
        password: "{{ lookup('env', 'VMWARE_PASSWORD') }}"
        validate_certs: "{{ vmware_validate_certs }}"
      register: tlb_esxi_roles
      no_log: true

    - name: Confirm VMware API discovery completed
      ansible.builtin.debug:
        msg:
          status: "VMware API discovery completed for {{ inventory_hostname }}."
          role_names: "{{ tlb_esxi_roles.local_role_info | map(attribute='role_name') | list }}"
          admin_privileges: >-
            {{ (tlb_esxi_roles.local_role_info
                | selectattr('role_name', 'equalto', 'Admin')
                | map(attribute='privileges') | first | default([])) }}
