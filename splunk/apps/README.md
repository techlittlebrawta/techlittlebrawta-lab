# Splunk applications

No complete Splunk applications have been migrated yet. This directory is
reserved for applications that provide dashboards, searches, workflows, or
other user-facing Splunk features.

Every future app should include a README, versioned `default/` configuration,
tests where practical, and no credentials or environment-specific `local/`
configuration.
